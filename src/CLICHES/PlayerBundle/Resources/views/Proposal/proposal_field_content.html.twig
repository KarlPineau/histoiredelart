{# src/HDA/ClichesBundle/Resources/views/Oeuvre/register_content.html.twig #}

<form class="form-horizontal" method="post" {{ form_enctype(form) }} role="form">
    {% if not form.vars.valid %}
    
    <div class="alert alert-error">
        <strong>Votre formulaire contient les erreurs suivantes :</strong>
        {{ form_errors(form) }}
        {{ form_errors(form.playerProposalFields) }}
    </div>
    <br />
    {% endif %}
    
    <fieldset>
        <legend>Cartel :</legend>
        
        {{ form_row(form.playerProposalFields) }}
        <div id="ajax-loader" class="text-center"><i class="fa fa-4x fa-spin fa-circle-o-notch" aria-hidden="true" alt="ajax-loader-image"></i></div>
    </fieldset>
    {{ form_rest(form) }}
    <br />
    <div class="pull-right"><a href="{{ path('cliches_player_end_end', {'playerSession_id' : playerProposal.playerOeuvre.playerSession.id }) }}" class="btn btn-default" title="Terminer la partie" data-toggle="tooltip" data-placement="left" ><span class="glyphicon glyphicon-log-out"></span></a></div>
    <div class="text-center"><input type="submit" class="btn btn-success" value="Vérifier" /></div>
</form>