<form class="form-horizontal" method="post" action="{{ path('data_data_entity_edit_sameas', {'id': entity.id}) }}" {{ form_enctype(form) }} role="form">
    {% if not form.vars.valid %}
    <div class="alert alert-error">
        <strong>Votre formulaire contient les erreurs suivantes :</strong>
        {{ form_errors(form) }}
        {{ form_errors(form.sameAs) }}
    </div>
    <br />
    {% endif %}
    
    <fieldset>
        <legend>SameAs :</legend>

        <div class="">
            {{ form_row(form.sameAs) }}
        </div>
    </fieldset>
        <br />
    {{ form_rest(form) }}
    <div class="text-center"><input type="submit" class="btn btn-default" /></div>
</form>